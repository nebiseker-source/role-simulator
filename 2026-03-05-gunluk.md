# 2026-03-05 Gunluk

## Bugun Yapilanlar
- `simulate-role` endpointine Ollama + RAG entegrasyonu eklendi.
- Akis: `task + role` ile playbook top-k + RAG top-k cekilip LLM'e verilecek sekilde guncellendi.
- Mermaid diyagram parse hatasi duzeltildi (`end` class ismi `finish` olarak degistirildi).
- RAG retrieval mantigi guclendirildi: eslesme yoksa ust siradaki bolumler fallback olarak geliyor.
- RAG tarafina rol bazli metadata eklendi (`business_analyst`, `product_owner`, vb. + `shared`).
- RAG indexleme ve arama endpointleri role filtre destekleyecek sekilde guncellendi.
- `simulate-role` cikti akisina JSON sema + parser katmani eklendi.
- LLM JSON donerse standart/profesyonel markdown rapora cevrilecek sekilde duzenlendi.
- UI'ye `Jira CSV Indir` ozelligi eklendi.
- RAG'e kaynak ekleme isteginde secili rol bilgisi gonderilecek hale getirildi.
- Lint ve type-check calisti:
  - `npm run lint`
  - `npx tsc --noEmit`

## Bugun Atilan Commitler
- `8242af5` - playbook retrieval fallback iyilestirmesi
- `6509bd0` - `extract-notes` GET/OPTIONS ve dynamic duzeltmesi
- `3b34d06` - yapilandirilmis/profesyonel output iyilestirmesi
- `0ecb401` - Mermaid parse hata duzeltmesi
- `0199981` - `simulate-role` Ollama + RAG entegrasyonu
- `d6c96a6` - JSON parser + Jira CSV + role-based RAG

## Kaldigimiz Asama
- Tek rol simulasyonu artik:
  - role + task + playbook + role-filtered RAG baglami ile calisiyor,
  - LLM (Ollama) uzerinden cikti uretiyor,
  - yapisal parser ile daha stabil rapor veriyor,
  - Jira CSV export alabiliyor.
- Sonraki buyuk adim: Jira CSV'yi daha ileri alanlarla genisletmek (Epic Link, Story Points, Sprint, Components).

## Yarin Projeyi Acarken Checklist
1. Terminal 1 (Ollama):
   - `ollama serve`
2. Model kontrol:
   - `ollama list`
   - gerekli modeller:
     - `qwen2.5:7b-instruct`
     - `nomic-embed-text`
3. Proje dizini:
   - `cd C:\dev\role-sim` (tercih edilen klasor) veya aktif calistigin repo klasoru
4. Ortam dosyasi (`.env.local`) kontrol:
   - `LLM_PROVIDER=local`
   - `OLLAMA_BASE_URL=http://127.0.0.1:11434`
   - `OLLAMA_MODEL=qwen2.5:7b-instruct`
   - `OLLAMA_EMBED_MODEL=nomic-embed-text`
5. Uygulamayi ac:
   - PowerShell ise: `npm.cmd run dev`
   - Gerekirse: `$env:NEXT_DISABLE_TURBOPACK=1`
6. Git durum kontrol:
   - `git status`
   - yeni gelismelerde commit + push:
     - `git add .`
     - `git commit -m "..."`
     - `git push origin main`
7. Vercel:
   - `main` push sonrasi auto-deploy durumunu kontrol et.
8. Uygulama smoke test:
   - Rol sec + gorev gir + `Tek Rol Simule Et`
   - Dosya yukle + `Kaynagi RAG'e Ekle`
   - `Jira CSV Indir` calisiyor mu kontrol et

## Notlar
- `C:\dev\role-sim` kopyasi tek basina guvenlik acigi degildir.
- `.env.local` dosyasinda API key yoksa risk dusuktur (su an local ollama ayarlari var).
- Git "dubious ownership" uyarisi guvenlik acigi degil, Git koruma mekanizmasidir.
